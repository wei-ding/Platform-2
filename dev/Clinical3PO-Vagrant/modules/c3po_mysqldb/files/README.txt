Unix

1) edit db.sh and set the variables USERNAME, HOST_NAME, DB_NAME, PASSWORD appropriately.
2) execute db.sh using sh db.sh

Windows

1) edit db.bat and set the variables USERNAME, HOST_NAME, DB_NAME, PASSWORD appropriately.
2) Run db.bat at the shell.